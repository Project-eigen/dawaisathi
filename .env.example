# Google Gemini API key (free tier). Get one in ~30s at:
#   https://aistudio.google.com/apikey
# No credit card required. Used server-side only.
GEMINI_API_KEY=

# Optional: override the model. Default is gemini-2.5-flash (free tier, good OCR).
# GEMINI_MODEL=gemini-2.5-flash
